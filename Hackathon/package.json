{
  "name": "secure-document-workflow",
  "version": "1.0.0",
  "description": "Secure document workflow with encryption, IPFS storage, and blockchain anchoring",
  "private": true,
  "workspaces": [
    "backend",
    "blockchain",
    "client"
  ],
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../blockchain && npm install && cd ../client && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:blockchain": "cd blockchain && npx hardhat node",
    "deploy:contract": "cd blockchain && npx hardhat run scripts/deploy-document-hash.js --network localhost",
    "build:backend": "cd backend && npm run build",
    "build:client": "cd client && npm run build",
    "test:backend": "cd backend && npm test",
    "test:client": "cd client && npm test",
    "test:all": "npm run test:backend && npm run test:client",
    "clean": "rm -rf backend/node_modules backend/dist client/node_modules client/dist blockchain/node_modules blockchain/artifacts blockchain/cache",
    "setup": "npm run install:all && echo '\n✅ Installation complete! See QUICKSTART.md for next steps.'"
  },
  "keywords": [
    "encryption",
    "blockchain",
    "ipfs",
    "document-management",
    "web3",
    "security"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
